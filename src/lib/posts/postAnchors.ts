// Shared anchor ids used to navigate within the post page. The element that
// owns each id (the post title, the comments header) and the Table of Contents
// links / scroll-spy that target them must agree, so the values live here
// rather than as magic strings in each component.
export const POST_TOP_ANCHOR = "top";
export const POST_COMMENTS_ANCHOR = "comments";
