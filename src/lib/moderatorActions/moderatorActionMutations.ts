import type { ModeratorActionType } from "./moderatorActionHelpers";

export const createModeratorAction = async (
  userId: string,
  moderatorActionType: ModeratorActionType,
) => {
  // TODO
  void userId;
  void moderatorActionType;
};
