import type { ForumEventBase } from "@/lib/forumEvents/forumEventQueries";

export default function ForumEventPoll({}: Readonly<{
  event: ForumEventBase;
  hideViewResults?: boolean;
  className?: string;
}>) {
  return null;
}
